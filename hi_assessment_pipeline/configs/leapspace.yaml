# Leapspace

# Run: python run_pipeline.py --config configs/leapspace.yaml
 
usecase: leapspace
csv_path: use_cases/leapspace/extractionsheet.csv
ontology_path: ontology/hi-ontology.ttl
output_dir: use_cases/leapspace/output
 
# Phase 1 block enables automated knowledge acquisition via Claude + web search
# csv_path will be created by Phase 1 and does not need to exist beforehand.
#phase1:
#  target_system: "Leapspace"
#  anthropic_model: claude-opus-4-5
#  max_searches: 60

